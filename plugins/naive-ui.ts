/**
 * Plugin de Naive UI para SSR
 * Simplificado para Nuxt 4
 */
export default defineNuxtPlugin(() => {
  // El módulo nuxtjs-naive-ui ya maneja el SSR correctamente
  // Este plugin solo asegura que esté disponible
});
